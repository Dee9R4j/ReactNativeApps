/**
 * Common constants and configuration
 */

// Mock backend URLs — these won't be called since we use mock APIs
// but are kept for future real backend integration
export const BACKEND_URL = "https://api.oasis.example.com";
export const AUTH_URL = "https://auth.oasis.example.com";

// App constants
export const APP_NAME = "Oasis";
export const APP_VERSION = "1.0.0";

// Store/Admin user IDs for special access
export const Commons = {
  IDs: [1001, 1002, 1003], // Admin/store user IDs
};
